__pycache__/
*.pyc
venv/
.venv/
.idea/

# Training artifacts (regenerated by train.py / dataset_report.py, not source).
weights/
runs/
reports/
tokenizer.json
*.pt

# NOT ignored: sample_data/ -- the small (~10 row) sample for the submission
# zip is meant to be committed (see export_sample.py).
